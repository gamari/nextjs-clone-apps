# 概要

共通で利用できる処理をまとめる。

## theme.ts

- tokens
  - mode => Color
- themeSettings
  - palette
- ColorModeContext

## CommonThemes

```ts
export const flexBetween = {
    display: 'flex',
    justifyContent: 'space-between',
};
```
